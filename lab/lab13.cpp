#include <iostream>    

using namespace std;

// Student ID 1:
// Student ID 2: 



// Write a function called shorty that takes an array of strings (and the length
// of the array) and returns the shortest string (if there is more than one
// shortest string, any one of them will do).


// Write a function called ceiling that takes an array of doubles (and the
// length of the array) and another double max and returns the average of the
// elements that are greater than max.



int main () {
  cout << boolalpha;

  
  cout << endl;

  return 0;
}